node_modules/
build/
*.log
.env
.env.keys

# ============================================
# Security-Related Additions
# ============================================

# Local environment files with secrets (CRITICAL)
.env.local
.env.*.local
.env.production.local
.env.development.local

# Docker override files that might contain secrets
docker-compose.override.yml
docker-compose.local.yml

# Backup files that might contain old secrets
*.backup
*.bak
.env.backup
.env.bak

# Secret files
secrets/
.secrets/
*.key
*.pem
*.crt

# IDE files that might contain sensitive data
.idea/
.vscode/settings.json
.vscode/launch.json

# Logs that might contain sensitive information
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
